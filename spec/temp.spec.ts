// import { SpecReporter } from "@ez-budgets/jasmine-junit-reporter";
//
describe("Temporary", () => {
	// beforeAll(() => {
	// 	const specReporter = new SpecReporter({
	// 		fileName: 'jasmine.xml',
	// 		dirName: './test-results'
	// 	});
	// 	jasmine.getEnv().addReporter(specReporter);
	// })
	it("should pass", () => {
		expect(true).toBeTrue();
	})
})